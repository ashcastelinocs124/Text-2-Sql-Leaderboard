# Text-2-SQL Agent Benchmark Scenario
# Green Agent: AgentX SQL Benchmark Evaluator
# Triggered: 2026-01-15

[green_agent]
# Use public AgentX green agent image
image = "keshavdalmia10/agentx-green:latest"
env = {}

# Participant - SQL generating agent
[[participants]]
# Use public AgentX purple agent image
image = "keshavdalmia10/agentx-purple:latest"
name = "sql_agent"
env = { GOOGLE_API_KEY = "${GOOGLE_API_KEY}" }

# Assessment Configuration
[config]
difficulty = ["easy", "medium"]
task_count = 5
scorer_preset = "default"
dialect = "sqlite"
same_tasks = true
# Re-trigger: Thu Jan 15 17:44:06 CST 2026
# Retry: 195456
# Retry: 195902
# Retry: 200317
